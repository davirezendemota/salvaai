# Obtenha o token em https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID do Telegram da conta que pode enviar cookies.txt (obrigatório para aceitar envio de cookies).
# Descubra seu ID com @userinfobot no Telegram.
TELEGRAM_ALLOWED_USER_ID=123456789

# Redis para fila de downloads (obrigatório)
REDIS_URL=redis://redis:6379/0

# Resumo de vídeo: transcrição (Whisper) + resumo e hashtags (GPT). Se definido, o caption enviado será o resumo em vez da descrição bruta.
# OPENAI_API_KEY=sk-...
# Opcional: desative com false para usar só a descrição do post como caption.
# ENABLE_VIDEO_SUMMARY=true

# SQLite para usuários, planos, recargas e whitelist (default: ./data/salvaai.db)
# DATABASE_URL=sqlite:///./data/salvaai.db

# Gateway de pagamento: example (stub) por enquanto
# PAYMENT_GATEWAY=example

# Ambiente teste: saldo de posts para todos os usuários (ex: 1000). Deixe vazio em produção.
# TEST_BALANCE_POSTS=1000

# Porta do webhook de pagamentos (default: 8080)
# WEBHOOK_PORT=8080

# IDs do Telegram na whitelist (não precisam pagar), separados por vírgula
# TELEGRAM_WHITELIST_USER_IDS=111,222
